@if (Attribute is not null)
{
	<MudTextField @bind-Value="CurrentValue" Label="@FormatLabel(Attribute.Name)" Variant="Variant.Outlined" Margin="Margin.Dense" AutoGrow="true">@Attribute.Value.ToString()</MudTextField>
}
